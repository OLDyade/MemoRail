# @memorail/core

Core file-native memory runtime for MemoRail.

It provides:

- Markdown memory schema validation
- safe file reads and writes inside a memory root
- rebuildable runtime indexes
- context construction
- read-only doctor checks

This package does not depend on a model provider, agent product, or hosted
service.
